<Page
    x:Class="UET_CODERANK.UI.NotificationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource MainBackground}">

    <Grid Padding="40" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Notifications" Foreground="White" FontSize="28" FontWeight="Bold"/>
            <TextBlock x:Name="txtSubtitle" Foreground="#888888" FontSize="13"/>
        </StackPanel>

        <Button Grid.Row="1" Content="Mark All as Read" Padding="16,8"
                CornerRadius="8" Click="btnMarkAllRead_Click"/>

        <Border Grid.Row="2" Background="#25282c" CornerRadius="12" Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="notificationRows"/>
            </ScrollViewer>
        </Border>

    </Grid>
</Page>