<Page
    x:Class="UET_CODERANK.UI.EnrollmentRequestsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource MainBackground}">

    <Grid Padding="40" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Enrollment Requests" Foreground="White" FontSize="28" FontWeight="Bold"/>
            <TextBlock x:Name="txtSubtitle" Foreground="#888888" FontSize="13"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <Button x:Name="btnAll" Content="All" Padding="16,8" CornerRadius="8" Click="btnFilter_Click"/>
            <Button x:Name="btnPending" Content="Pending" Padding="16,8" CornerRadius="8" Click="btnFilter_Click"/>
            <Button x:Name="btnApproved" Content="Approved" Padding="16,8" CornerRadius="8" Click="btnFilter_Click"/>
            <Button x:Name="btnRejected" Content="Rejected" Padding="16,8" CornerRadius="8" Click="btnFilter_Click"/>
        </StackPanel>

        <Border Grid.Row="2" Background="#25282c" CornerRadius="12" Padding="0">
            <StackPanel>
                <Grid Padding="20,14" Background="#1e2124">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="180"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Student" Foreground="#888888" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Batch" Foreground="#888888" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Section" Foreground="#888888" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="Requested" Foreground="#888888" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Text="Status" Foreground="#888888" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="5" Text="Action" Foreground="#888888" FontSize="12" FontWeight="SemiBold"/>
                </Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="600">
                    <StackPanel x:Name="requestRows"/>
                </ScrollViewer>
            </StackPanel>
        </Border>

    </Grid>
</Page>